struct object_data_t
{
    int64_t utime;

    int32_t id;

    int32_t num_cat;
    categorized_data_t cat_dat[num_cat];

    double pos[6]; // xytrpy
    double bbox[3][2];// Min max axis aligned
}

arm_driver {
    // Specifiy PID controllers for servo ids
    // format: pid# = [IDNUM, p, i, d]
    pids {
        pid# = [0, 16, 2, 1];
        pid# = [1, 12, 6, 0];
        pid# = [2, 12, 6, 0];
    }
}
